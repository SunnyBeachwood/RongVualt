package org.eds.veracrypt.ui

/** Internal launch contract shared by the EDS entry points and file manager. */
enum class FileManagerLaunchMode {
    BROWSE,
    PICK_CONTAINER,
    OPEN_UNLOCKED_VOLUME
}
